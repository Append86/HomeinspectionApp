alerts:
- rule: DEPLOYMENT_FAILED
- rule: DOMAIN_FAILED
databases:
- cluster_name: db-postgresql-nyc3-04821
  db_name: defaultdb
  db_user: doadmin
  engine: PG
  name: db-postgresql-nyc3-04821
  production: true
features:
- buildpack-stack=ubuntu-22
ingress:
  rules:
  - component:
      name: homeinspectionapp-backend
      preserve_path_prefix: true
    match:
      path:
        prefix: /api
  - component:
      name: homeinspectionapp-frontend
      preserve_path_prefix: true
    match:
      path:
        prefix: /
name: home-inspection-tool
region: nyc
services:
- dockerfile_path: backend/Dockerfile
  envs:
  - key: DATABASE_URL
    scope: RUN_AND_BUILD_TIME
    value: postgresql://doadmin:AVNS_P9WT2R5-BQ7CNghduXg@db-postgresql-nyc3-04821-do-user-35252860-0.l.db.ondigitalocean.com:25060/defaultdb?sslmode=require
  - key: DJANGO_SECRET_KEY
    scope: RUN_AND_BUILD_TIME
    value: '!!django-insecure-nsi)!!@!24ra2op(0j=23vo!!nq(anxj($3fu%t!7%ffqtrp008h-jt'
  - key: DJANGO_DEBUG
    scope: RUN_AND_BUILD_TIME
    value: "false"
  - key: ALLOWED_HOSTS
    scope: RUN_AND_BUILD_TIME
    value: home-inspection-tool-ylcht.ondigitalocean.app
  - key: VITE_API_URL
    scope: RUN_AND_BUILD_TIME
    value: home-inspection-tool-ylcht.ondigitalocean.app
  - key: STATIC_URL
    scope: RUN_AND_BUILD_TIME
    value: /api/static/
  - key: CORS_ALLOWED_ORIGINS
    scope: RUN_AND_BUILD_TIME
    value: https://home-inspection-tool-ylcht.ondigitalocean.app
  - key: CSRF_TRUSTED_ORIGINS
    scope: RUN_AND_BUILD_TIME
    value: |
      https://home-inspection-tool-ylcht.ondigitalocean.app
 
    
    
  http_port: 8080
  instance_count: 1
  instance_size_slug: apps-s-1vcpu-0.5gb
  name: homeinspectionapp-backend
  run_command: gunicorn core.wsgi --bind 0.0.0.0:8080
  source_dir: backend
static_sites:
- build_command: npm run build
  catchall_document: index.html
  environment_slug: node-js
  
  
  name: frontend
  output_dir: dist
  source_dir: frontend
  error_document: index.html
